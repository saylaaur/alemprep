# Паспорт выпуска: блок 0

Проверка: 08.09.2026 (UTC). Статус: **в работе; ученический запуск не принят**.

## Что подтверждено

| Проверка | Наблюдение | Что это доказывает |
| --- | --- | --- |
| Локальный main и GitHub main | `a97a62e54baa0b00d7231169b9ec1e851b18756b` | Основные ветки совпадают на момент проверки |
| Ветка безопасности | `codex/production-safety`, `68a7c238d7488e8f6e33d643fd0abe8a9f76b7eb`, `/private/tmp/alemprep-production-safety`; чистая | 10 коммитов, 13 файлов сверх main, +267/−16 строк |
| Возможность объединения | main — предок ветки; `git diff --check main..codex/production-safety` без ошибок | Историю можно объединить fast-forward; пользовательские незакоммиченные файлы нужно сохранить |
| Последний Production в GitHub Deployments | ID `6292424109`, SHA `a97a62e…`, создан 06.09.2026 11:33:07 UTC, status `success` | Последняя зарегистрированная публикация не содержит 10 исправлений |
| CI старого main | [Verify](https://github.com/saylaaur/alemprep/actions/runs/34030533166/job/101479089630), success | Это проверка старого коммита, не текущей ветки безопасности |
| Живая схема Supabase | GET `sessions?select=question_ids&limit=0`: HTTP 200, пустой результат | Столбец из 0022 доступен REST API; строки пользователей не читались |
| Effective grants и RLS | Read-only SQL 09.09: RLS включён на `profiles`, `sessions`, `attempts`; authenticated не может UPDATE `xp`, стрик или `is_admin`, но может UPDATE sessions и INSERT/DELETE attempts | 0023 действует на важные вычисляемые поля; пользователь всё ещё может подделать собственные session/attempt данные через прямой API |
| Manifest 0022 | Read-only SQL 09.09: `sessions.question_ids` — UUID-массив; `sessions_question_ids_count_check` существует | Структура и constraint применены в живой БД |
| Миграционный ledger | `supabase_migrations.schema_migrations` отсутствует | Применение миграций нельзя подтвердить обычным ledger; подтверждаем фактическими схемой, grants и smoke |
| Публичный адрес | `https://alemprep.vercel.app/ru` и `/kk`: HTTP 200 | Публичные страницы доступны; точный SHA доменного alias этим не определяется |
| Гостевой доступ | `/ru/dashboard`, `/kk/dashboard`: HTTP 307 на соответствующий login | Гостевая защита этих двух адресов сработала |

Источник метаданных публикации: GitHub API `repos/saylaaur/alemprep/deployments` и `deployments/6292424109/statuses`. Deployment URL: `https://alemprep-ajinv84q3-saylaaurs-projects.vercel.app`. Прямой запрос туда возвращает Vercel SSO, HTTP 302. Доступ к Vercel control plane и привязка публичного alias к SHA не проверены. Нельзя объявлять точный активный SHA домена только по последнему GitHub deployment.

В `.env.local` текущего основного checkout есть только `NEXT_PUBLIC_SUPABASE_URL`; anon key, service-role key и site URL в нём не заданы. Это не доказывает ошибку production, но блокирует локальный smoke с синтетическим пользователем. Значения секретов не выводились и не менялись. Ветка проверялась с явно заданными фиктивными CI-переменными, без production-секретов.

## Свежие проверки ветки 68a7c23

Окружение: Node `v24.15.0`, npm `11.12.1`; CI настроен на Node 22. Установленные зависимости worktree использованы без переустановки.

| Команда | Результат |
| --- | --- |
| `npm run typecheck` | PASS, exit 0, обе конфигурации TypeScript |
| `npm run lint` | PASS, exit 0, предупреждений нет |
| Профильные тесты practice/diagnostic/weekly/middleware | PASS, 4 файла, 57 тестов |
| `npm test` | PASS, 36 файлов, 485 тестов |
| `npm run build` | Не принят: сначала недоступны Google Fonts; после разрешения сети Turbopack остановился на `binding to a port: Operation not permitted` |
| `npm run build -- --webpack` | PASS, exit 0, TypeScript и 25 статических страниц; диагностическая сборка, не замена обязательной стандартной сборки CI |

Ошибка Turbopack возникла при обработке `app/globals.css`/PostCSS, на запуске процесса с локальным портом. Исходники ради обхода не менялись. Нужен обычный build в CI/окружении, где разрешён этот механизм. Успех Webpack не означает, что стандартная команда уже проверена.

## Совместимость и оставшиеся риски

1. **0023 и старый main.** Effective grants подтверждают: authenticated не может обновлять XP/стрик. Старый main обновляет эти поля через пользовательский Supabase-клиент, поэтому такой UPDATE отклонится, а сохранение вернёт ошибку. Ветка переносит записи на server-side service-role клиент. Не возвращать клиенту права XP для устранения несовместимости.
2. **0022 подтверждена по схеме.** UUID-массив и constraint существуют; миграционного ledger нет. Старые сессии с NULL остаются без зафиксированного набора. Перед допуском определить срок жизни/завершение старых сессий; не допускать NULL как способ обхода проверки.
3. **Доверенные результаты ещё не обеспечены полностью.** Effective grants подтверждают UPDATE sessions и INSERT/DELETE attempts для authenticated. В `pg_policies` одновременно есть широкие `sessions_own`/`attempts_own` с `ALL` и точечные политики. Проверка владельца не запрещает подмену своего score/is_correct/manifest. Эффективные production-права и поведенческие проверки нужны в блоке 2.
4. **Server actions тоже публичная поверхность.** `createExamSession` экспортируется из `'use server'` и принимает состав вопросов. До заявлений о серверной выдаче нужно проверить прямой вызов, режим сессии в `finishExamSession`, подмену состава, отсутствие manifest и границы входных массивов.
5. **Частичные записи и гонки.** Попытки, итог и XP записываются несколькими запросами с компенсациями. Исключение, потеря связи или две вкладки могут оставить рассогласование. Требуется транзакционная операция и идемпотентность, затем аудит в той же транзакции.
6. **Cache-Control на гостевом редиректе.** Публичный ответ на dashboard гостя содержит `public, max-age=0, must-revalidate`; это не доказательство утечки личной страницы. Проверка авторизованных HTML/RSC, выхода и перехода между аккаунтами остаётся обязательной.

Это не полный аудит безопасности. Новые школьные роли, журнал действий и Desmos ещё не реализованы.

## Как завершить блок 0

- [x] Выполнить [check-release.sql](check-release.sql) в SQL Editor собственного проекта. Скрипт подтвердил effective grants/RLS и schema 0022; ledger миграций отсутствует. SQL Editor сохранил несохранённый private query пользователя, существующие запросы не менялись.
- [ ] В Vercel подтвердить активный production deployment/alias/SHA и наличие необходимых переменных без показа значений. В доступных API нет Vercel token; нативная вкладка Supabase обнаружена, но содержимое SQL Editor не удалось прочитать, запрос там не выполнялся.
- [ ] Проверить короткий сценарий на выделенном синтетическом аккаунте: RU/KK вход → старт сессии → ответ → итог → обновление страницы → выход. Проверить сохранённый результат, а не только экран успеха. Не использовать существующую историю ученика для эксперимента. Попытка запустить такой smoke 09.09 не сделала записей: в текущем `.env.local` нет anon/service-role ключей.
- [ ] Получить зелёный стандартный build для ветки. Для CI использовать Node 22 и `npm ci --legacy-peer-deps`; имеющийся workflow выполняет typecheck/lint/test/build на PR и main.
- [ ] Зафиксировать параметры школ, когда организатор их получит. Сейчас подтверждены только две сельские школы и RU/KK.

## Порядок выпуска и отката

1. Сохранить текущие значения deployment/SHA и отчёт grants; проверить, какие из 0020–0023 уже применены. Не выполнять миграции повторно вслепую: 0022 повторно добавляет именованный constraint.
2. Получить зелёный CI и smoke на тестовой среде с эквивалентными схемой и правами. Проверить обработку отсутствующего server-side ключа и старых NULL-manifest сессий. Закрыть несовместимости до допуска учеников.
3. Подготовить fast-forward `main` к проверенной ветке, сохранив рабочие документы/презентации; после любых изменений main пересчитать совместимость. Само объединение и публикация в этом проходе не выполнены.
4. Опубликовать точный проверенный commit, подтвердить alias, повторить smoke, затем наблюдать ошибки сохранения. Если старая схема/код уже несовместимы, приоритет — исправленный выпуск, а не новые функции.
5. Откатывать приложение только к коммиту, который совместим с действующими grants и manifest. Старый `a97a62e` нельзя считать безопасной точкой отката при действующей 0023. Если совместимого выпуска нет — временно остановить запись/занятие и выпустить исправление вперёд; не восстанавливать клиентскую запись XP и не откатывать production-БД вслепую.

**Итог прохода:** исходное состояние восстановлено частично, проверки кода и effective grants выполнены, риски и оставшиеся доказательства перечислены. Код приложения, права БД, production deployment и секреты не менялись. Блок 0 целиком не закрыт, новая реализация следующих блоков не начата.
