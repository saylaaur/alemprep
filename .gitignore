node_modules
.next
out
build
dist
*.tsbuildinfo

# Python (venv, если вдруг создаётся в папке — проекту не нужен)
.venv
__pycache__/

.env
.env.local
.env.*.local

.DS_Store
*.log

next-env.d.ts

# Content pipeline outputs (runtime artefacts, not committed)
scripts/references/
scripts/generated/
scripts/verified/
scripts/insert-batch.sql

# Исходные материалы пробников (скрины НЦТ) — НЕ коммитить в публичный репо.
# Репозиторий ПУБЛИЧНЫЙ, материалы НЦТ защищены авторским правом.
# Варианты написания папки покрыты намеренно (пробел/дефис/опечатка «recources»).
physics/
physics */
unt-resources/
unt recources/
unt*resources/
unt*recources/
*.unt/
